
import { Product, OfficeUnit, Customer } from './types';

export const DEFAULT_PASSWORD = '123';
export const BASE_MONTHLY_FEE = 150.00;
export const REFERRAL_DISCOUNT = 1.00;

// Iniciando sistema totalmente limpo conforme solicitado
export const MOCK_PRODUCTS: Product[] = [];
export const MOCK_OFFICES: OfficeUnit[] = [];
export const MOCK_CUSTOMERS: Customer[] = [];
